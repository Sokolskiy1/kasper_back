[//]: # (Запуск возможен через python script.py)
Запуск можно осуществить за счет двух команд
docker-compose down -v
docker-compose up --build
Создание docker-compose, в котором будет бэкенд и бд(данные из dump)